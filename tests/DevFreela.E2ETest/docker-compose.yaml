version: '3.4'

services:
  codeflic.catalog.db:
    container_name: devfreela-e2e
    hostname: devfreela-e2e
    image: mysql
    ports:
    - 33060:3306
    environment:
      MYSQL_ROOT_PASSWORD: root